#if(!$package.isEmpty())
package $package;
#end

#foreach($importClass in $importClassList)
import $importClass;
#end

public interface ${entitySimpleName}Service extends ${superClassName}<${entitySimpleName}> {

}